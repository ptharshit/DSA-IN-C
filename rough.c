#include <stdio.h>
#include <stdlib.h>
struct node{
    int data;
    struct node* next;
};
typedef struct node Node;
int main()
{
    // int a=1;
    // do
    // {
    //     printf("%d\t",a);
    //     a++;
    // } while (a!=2);
    
    Node* head=(Node*)malloc(sizeof(Node));
    return 0;
}